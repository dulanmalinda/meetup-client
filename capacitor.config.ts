import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.play90s.meetup',
  appName: 'meetup',
  webDir: 'dist/meetup',
  bundledWebRuntime: false
};

export default config;
